# 2026-07-24

## publish-post 页面优化

- 二手挂单价格输入框：增加 `priceFocused` 状态，focus 时动态清空 placeholder，实现"只显示光标"效果。
- 图文帖子：
  - 标题增加必填红色 * 标识。
  - 内容输入区下方新增"如题"快捷按钮，点击后内容自动填入"如题"。
- 二手挂单默认选中"二手书"（`selectedIdleType` 初始值改为 `'book'`）。
